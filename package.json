{
  "name": "identity-tokens-evm",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy": "forge script script/Deploy.s.sol:Deploy --broadcast",
    "format": "forge fmt"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}